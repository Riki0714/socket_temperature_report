相关说明：
  一个简单的树莓派温度上报服务器test

（1）主要代码都放在了base文件夹下，此处没有做.c和.h文件的分别存放，请见谅。
（2）在base文件夹下有本人写的makefile，请配合make命令进行服务器和客户端的开启，
  例：   启动服务器：make server
         启动客户端：make client
  PS: 命令行参数的修改在makefile中修改相关语句更为方便

（3）目前存在的bug：
    代码写的不好，且有些地方代码冗余。
    没有进行几十上百个客户端同时连接服务器的测试，可能存在bug。
    在先开启服务器，再开启客户端时，服务器出错恢复后客户端都可重新连接上；但先开启客户端，再开启服务器时，有时会出现服务器运行正常而客户端卡死的情况，应是代码逻辑的问题。（已修复，但可能还是存在bug）
    broken pipe问题出现时的错误数据没有进行清理（或许可以用setsockopt函数参数的设置避免broken pipe的情况出现）
